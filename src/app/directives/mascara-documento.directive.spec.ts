import { MascaraDocumentoDirective } from './mascara-documento.directive';

describe('MascaraDocumentoDirective', () => {
  it('should create an instance', () => {
    const directive = new MascaraDocumentoDirective();
    expect(directive).toBeTruthy();
  });
});
